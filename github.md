repo: adrian-degraaf/My-finance
branch: main

## Last sync

date: 2026-08-17T10:21:20Z

The repository was empty at the time of connecting — the tree endpoint returned
409, meaning no commits on `main`. Nothing was imported from it; the association
was made so this project has a recorded home. Files were handed to the user as a
download to commit from their own clone.

### Updated in this project

- Connected the repository; found it empty, so nothing was pulled in.
- Prepared the project for a first commit: README, config split, .gitignore.
- Credentials moved out of both pages into a gitignored `config.js`.

## Screen map

| Screen | Built from |
| --- | --- |
| Finance Tracker.dc.html | Authored here, not from the repo |
| Budget Setup.dc.html | Authored here, not from the repo |
| supabase/*.sql | Authored here, not from the repo |
